apiVersion: v1
kind: Service
metadata:
  name: catalog-stable
  namespace: staging
spec:
  selector:
    app: catalog
  ports:
    - port: 80
      targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: catalog-canary
  namespace: staging
spec:
  selector:
    app: catalog
  ports:
    - port: 80
      targetPort: 8080
