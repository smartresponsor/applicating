name: observability
on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: php bin/obs-metric app_requests_total '{"tenant":"tenantA"}' 1 || true
