# Migration M3 -> M4
- Variant scale v2: ensure indexer paths and storage can handle >= 2048 variants
- Introduce promo-rule and tax-rule schemas; wire endpoints (OpenAPI) and stubs
- Add webhook event format + signature (HMAC-SHA256) for product/variant events
- Provide SDK skeletons (TS/PHP) for quicker client adoption
