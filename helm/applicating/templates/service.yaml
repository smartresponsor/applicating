apiVersion: v1
kind: Service
metadata:
  name: {{ include "applicating.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "applicating.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
