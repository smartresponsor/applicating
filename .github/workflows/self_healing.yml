name: self_healing
on:
  schedule:
    - cron: "*/20 * * * *"
  workflow_dispatch:
jobs:
  heal:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: php bin/heal-run tenantA || true
