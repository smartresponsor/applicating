# Applicating secret material

The active runtime keeps its Kubernetes secret placeholder under `k8s/sealedsecrets/applicating-secrets.yaml`.

Older catalog-named encrypted secret material was moved to `archive/repo-drift/sops-archive/` so it no longer appears to be part of the live Applicating operational contour.
