import React from 'react'
export default function ETL(){
  return (<section>
    <h2>ETL / Connectors</h2>
    <p>Источники: usage JSON, billing CSV/Stripe, CRM webhook. Стадии: staging → facts/dims → CDC.</p>
  </section>)
}
