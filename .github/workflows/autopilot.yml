name: autopilot
on:
  schedule:
    - cron: "0 2 * * *"
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Autopilot demo
        run: |
          php bin/autopilot-run tenantA 40,48,55,63,71 || true
          echo 'autopilot cycle simulated'
