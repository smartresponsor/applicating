name: security_compliance
on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:
jobs:
  retention:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: php bin/sec-retention-run || true
